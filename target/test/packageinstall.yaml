apiVersion: packaging.carvel.dev/v1alpha1
kind: PackageInstall
metadata:
  name: metallb-package
  namespace: metallb-package
spec:
  serviceAccountName: metallb-sa
  packageRef:
    refName: metallb.tkgdev.failk8s.com
    versionSelection:
      constraints: 1.0.0
      prereleases: {}
  values:
  - secretRef:
      name: metallb-config
